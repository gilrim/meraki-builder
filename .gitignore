# Integrated MS42P build workspace and artifacts
/.work/
/artifacts/

# Local donor and bootloader inputs
/inputs/*.bin
/inputs/*.rom
/inputs/*.img
/inputs/*.squashfs
/inputs/*.ko
/inputs/SHA256SUMS

# Legacy local binaries
nor/bin/*
nor/vmlinux*
nor/vmlinuz*
nor/*.bin
*.ko

# Build products
*.so
*.o

# Docker build directories
docker/*/src

# devenv
.devenv/
.devenv.flake.nix

# Nix build output
/build/
