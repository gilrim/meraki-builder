# Public NTP pool.
pool pool.ntp.org iburst

# Correct a badly wrong clock immediately during the first three updates.
makestep 1.0 3

# Synchronize the RTC through the kernel when supported.
rtcsync
