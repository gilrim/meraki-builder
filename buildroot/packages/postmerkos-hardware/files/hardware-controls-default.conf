# Immutable board-control registration. Physical paths belong here, not in the
# persistent JFFS2 security configuration.
RESET_BACKEND=unidentified
RESET_PATH=
RESET_DEVICE=
RESET_CODE=0
RESET_ACTIVE=0
RESET_VERIFIED=0

PORT_LED_BACKEND=click
PORT_LED_HANDLER=/click/sw0_ctrl/poe_led_state
PORT_LED_VERIFIED=1

STATUS_LED_BACKEND=unidentified
STATUS_LED_PATH=
STATUS_LED_ACTIVE=1
STATUS_LED_VERIFIED=0
